services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  litellm:
    # Pinned by digest (LiteLLM 1.102.0): policy/litellm_callback.py relies on
    # version-specific internals -- where request headers live, and whose
    # thinking/effort wins -- so a floating tag can silently change routing.
    image: ghcr.io/berriai/litellm:main-stable@sha256:32cfd7a427f6470033b4dcc9e75dfbea2aa32dbc298b0b511a40e962ce818362
    restart: unless-stopped
    depends_on:
      - postgres
    environment:
      LITELLM_MASTER_KEY: ${LITELLM_MASTER_KEY}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
    volumes:
      - ./litellm/config.yaml:/app/config.yaml
      - ./policy:/app/policy
      - ./catalog.yaml:/app/catalog.yaml
    ports:
      - "127.0.0.1:4000:4000"
    command: ["--config", "/app/config.yaml", "--port", "4000"]

volumes:
  postgres_data:
